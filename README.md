# wmbus-toolkit
Collect, archive and decrypt wireless M-Bus telegrams from wmbusmeters. Dedupes raw telegrams into CSV, builds AES key candidate lists (defaults + ID/serial-derived), and validates keys against collected traffic. Tested with Qundis Q water/heat 5.5. No hardcoded meter data.
